# Data

The original input data are not included in this repository because the Seurat object is large.

The analysis starts from a post-QC, integrated Seurat object:

all_integrated.rds

Expected local path before running scripts:

data/all_integrated.rds

This object contains axolotl limb regeneration single-cell RNA-seq data across multiple timepoints.
